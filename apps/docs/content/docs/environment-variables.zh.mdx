---
title: 环境变量
description: 自托管 Multica 常用的服务端、存储、集成和运行时配置。
---

import { Callout } from "fumadocs-ui/components/callout";

Multica 在进程启动时读取环境变量。修改后通常需要重启对应的 API、Web 或守护进程。Docker Compose 的 `docker compose restart` 不会重新读取 `.env`，要用 `up -d` 重建容器才能生效。

本页只列面向部署者的配置；测试变量和内部运行变量不在这里展开。本页是分组参考；完整部署步骤见[自托管快速上手](/self-host-quickstart)。

## 生产环境最小配置

```dotenv
DATABASE_URL=postgres://user:password@postgres:5432/multica?sslmode=require
JWT_SECRET=<long-random-secret>
APP_ENV=production
FRONTEND_ORIGIN=https://multica.example.com
MULTICA_APP_URL=https://multica.example.com
MULTICA_PUBLIC_URL=https://api.multica.example.com
```

还需要选择一种邮件服务，否则验证码和邀请只会写入服务端日志。

<Callout type="warning">
`JWT_SECRET` 在生产环境为必填：当 `APP_ENV=production` 时，后端会在密钥为空或等于已知占位值时拒绝启动（用 `openssl rand -hex 32` 生成）。也不要在生产环境设置 `MULTICA_DEV_VERIFICATION_CODE`。
</Callout>

## API 与数据库

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DATABASE_URL` | 本地 `multica` 数据库 | PostgreSQL 连接地址 |
| `DATABASE_MAX_CONNS` | `25` | 单个 API 进程的最大数据库连接数 |
| `DATABASE_MIN_CONNS` | `5` | 单个 API 进程保留的最小连接数 |
| `DATABASE_SEARCH_WORK_MEM_MB` | `64` | 每个搜索计划算子的 PostgreSQL `work_mem` 上限（MB）；设为 `1`–`64` 可调低，设为 `0` 则沿用数据库/会话默认值 |
| `DATABASE_REPLICA_URL` | 空 | 可选的 PostgreSQL 只读从库连接地址；新连接会强制校验只读状态，业务仍需在代码中显式选择最终一致读取 |
| `DATABASE_REPLICA_MAX_CONNS` | `10` | 单个 API 进程的最大从库连接数 |
| `DATABASE_REPLICA_MIN_CONNS` | `0` | 单个 API 进程保留的最小从库连接数 |
| `MULTICA_DATABASE_STARTUP_TIMEOUT` | `3m` | 容器启动过程中，migration 与 API 共享的暂时性数据库故障重试时长；设为 `0` 时每个阶段只尝试一次 |
| `MULTICA_DATABASE_CONNECT_TIMEOUT` | `5s` | 启动期间单次数据库连接的兜底超时；pgx 原生 `connect_timeout`、`PGCONNECT_TIMEOUT` 或 service 配置优先 |
| `PORT` | `8080` | API 监听端口 |
| `JWT_SECRET` | 生产环境必填 | 登录 JWT、部分签名流程使用的密钥；生产环境对空值或已知占位值拒绝启动 |
| `APP_ENV` | 空 | 生产环境设为 `production` |
| `AUTH_TOKEN_TTL` | `720h`（30 天） | 浏览器 JWT 与 cookie 有效期；接受 Go duration 或正整数秒数 |
| `LOG_LEVEL` | `debug` | 日志级别：`debug`、`info`、`warn` 或 `error`（大小写不敏感）；无法识别的值同样回退到 `debug` |
| `MULTICA_SHUTDOWN_HOLD_DURATION` | `0` | 收到终止信号后，开始优雅退出前等待多久 |
| `MULTICA_RUNTIME_RECONNECT_GRACE` | `3h` | 运行时离线后可重新连接且不终止在途运行的宽限时间；低于 `150s` 时按 `150s` 处理 |
| `MULTICA_RUN_UNRESPONSIVE_AFTER` | `90`（秒） | 一次活跃运行可以保持多久沉默才会被显示为无响应；这只是一个读取时的判断，不会让运行失败或被取消 |
| `MULTICA_BUDGET_MIN_ESTIMATE_USD_TICKS` | `1000000000`（0.10 美元） | 运行预算估算的下限，单位为 USD tick（1 美元 = 10,000,000,000 tick）；仅在已完成运行数量不足以从历史数据中平均出真实成本之前使用 |

在 Kubernetes 中设置 shutdown hold 时，`terminationGracePeriodSeconds` 要大于 hold 与实际退出所需时间之和。

主库与从库的连接上限彼此独立，需要按全部 API 进程的连接数总和预留 PostgreSQL 集群连接额度。从库连接默认五分钟回收一次，以便在提升后重新校验只读状态。从库连接失败时，基础设施会透明回退主库并短暂打开被动熔断器，不会增加后台数据库探测。应用不保证从库数据的最大陈旧时间；请在数据库层监控复制延迟，一致性敏感的读取继续走主库。

## 对外地址与浏览器访问

| 变量 | 默认值 | 说明 |
|---|---|---|
| `FRONTEND_ORIGIN` | 空 | 用户访问的前端 origin；用于 CORS、cookie 和邀请链接 |
| `MULTICA_APP_URL` | 回退到 `FRONTEND_ORIGIN` | 用户可访问的 Web 地址；CLI 登录和账号绑定链接使用它 |
| `MULTICA_PUBLIC_URL` | 空 | 公网 API 地址；用于 webhook URL 和运行时连接说明 |
| `MULTICA_DAEMON_SERVER_URL` | 回退到 `MULTICA_PUBLIC_URL`，再回退到 `MULTICA_APP_URL` / `FRONTEND_ORIGIN` | API 进程写入 `multica setup self-host` 命令的服务端地址；守护进程访问 API 的地址与公网 webhook 地址不同时设置 |
| `CORS_ALLOWED_ORIGINS` | 空 | 额外允许的 HTTP origin，逗号分隔 |
| `ALLOWED_ORIGINS` | 回退到 CORS / 前端地址 | WebSocket origin 白名单，逗号分隔 |
| `COOKIE_DOMAIN` | 空 | 前后端不同 host 且浏览器直接访问 API 域时必须设置；单域部署保持为空 |
| `NEXT_PUBLIC_API_URL` | 根据页面 origin 自动推导 | 浏览器和 Next.js SSR 代理调用的后端 origin（scheme + host，不含路径）；结尾的 `/api` 会被去掉，其他任何路径都会在每个请求里重复出现 |
| `NEXT_PUBLIC_WS_URL` | 根据页面 origin 自动推导 | 浏览器连接的 WebSocket 地址 |

<Callout type="warning">
`MULTICA_DAEMON_SERVER_URL` 会由无需认证的 `/api/config` 接口返回，客户端可以读取。请将它视为公开配置，切勿填入凭据、令牌或其他密钥。
</Callout>

前端和 API 使用不同 host、且浏览器直接访问 API 域时，必须设置 `COOKIE_DOMAIN`，否则浏览器读不到 CSRF cookie——所有写请求返回 `403 CSRF validation failed`，而读请求一切正常。取值用能同时覆盖两个 host 的最窄父域（`.agent.example.com` 优于 `.example.com`）。它会把登录会话 cookie 扩散到该域下的所有 host，只有这些 host 都由同一可信主体运维时才可用。修改后需要在两个 host 上清除旧 cookie 并重新登录。若采用[自托管快速上手](/self-host-quickstart#3-选择访问方式)的同源配方（浏览器只访问 app 域），保持为空即可。不要填写 IP 地址，浏览器会忽略带 IP Domain 的 cookie。

<Callout type="warning">
自托管部署需要设置 `FRONTEND_ORIGIN`。缺少它时，邀请链接、cookie 安全属性和 WebSocket origin 校验都可能与实际域名不一致。
</Callout>

## 邮件与登录

### Resend

| 变量 | 默认值 | 说明 |
|---|---|---|
| `RESEND_API_KEY` | 空 | 设置后启用 Resend |
| `RESEND_FROM_EMAIL` | `noreply@multica.ai` | 发件地址，必须属于已验证域名 |

### SMTP

只要 `SMTP_HOST` 非空，SMTP 就会优先于 Resend。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `SMTP_HOST` | 空 | SMTP 主机；设置后启用 SMTP |
| `SMTP_PORT` | `25` | 常见值：25、587、465 |
| `SMTP_USERNAME` | 空 | 用户名；匿名 relay 留空 |
| `SMTP_PASSWORD` | 空 | 密码 |
| `SMTP_FROM_EMAIL` | 回退到 `RESEND_FROM_EMAIL` | Envelope From 与邮件 From |
| `SMTP_TLS` | `starttls` | `implicit`、`smtps` 或 `ssl` 表示隐式 TLS；465 会自动启用 |
| `SMTP_TLS_INSECURE` | `false` | 跳过证书校验，仅用于可信内网 |
| `SMTP_EHLO_NAME` | 主机名 | 严格 relay 要求的 EHLO/FQDN |

### Google OAuth

| 变量 | 默认值 | 说明 |
|---|---|---|
| `GOOGLE_CLIENT_ID` | 空 | Google OAuth client ID |
| `GOOGLE_CLIENT_SECRET` | 空 | Google OAuth client secret |
| `GOOGLE_REDIRECT_URI` | `http://localhost:3000/auth/callback` | 必须与 Google Console 中的回调地址完全一致 |

### 注册范围

| 变量 | 默认值 | 说明 |
|---|---|---|
| `ALLOW_SIGNUP` | `true` | 未配置任何白名单时是否允许创建新账号 |
| `ALLOWED_EMAILS` | 空 | 允许注册的完整邮箱，逗号分隔 |
| `ALLOWED_EMAIL_DOMAINS` | 空 | 允许注册的邮箱域名，逗号分隔 |
| `DISABLE_WORKSPACE_CREATION` | `false` | 禁止所有用户新建工作区；没有 owner/admin 例外 |
| `MULTICA_DEV_VERIFICATION_CODE` | 空 | 非 production 环境使用的固定 6 位测试验证码 |

白名单的准确判断顺序见[登录与注册](/auth-setup#注册范围控制)。

## 附件存储

没有设置 `S3_BUCKET` 时，Multica 使用本地磁盘。

### S3 或兼容存储

| 变量 | 默认值 | 说明 |
|---|---|---|
| `S3_BUCKET` | 空 | Bucket 名称，不要填写完整 hostname |
| `S3_REGION` | `us-west-2` | Bucket 所在区域 |
| `AWS_ACCESS_KEY_ID` | SDK 默认凭据链 | 静态 access key |
| `AWS_SECRET_ACCESS_KEY` | SDK 默认凭据链 | 静态 secret key |
| `AWS_ENDPOINT_URL` | 空 | MinIO 等 S3 兼容 endpoint |
| `S3_USE_PATH_STYLE` | 自定义 endpoint 时为 `true` | 是否使用 path-style 地址 |
| `ATTACHMENT_DOWNLOAD_MODE` | `auto` | `auto`、`cloudfront`、`presign` 或 `proxy` |
| `ATTACHMENT_DOWNLOAD_URL_TTL` | `30m` | 签名下载地址的有效期 |

内网 MinIO 等 endpoint 无法被浏览器直接访问时，使用 `ATTACHMENT_DOWNLOAD_MODE=proxy`。

### 本地磁盘

| 变量 | 默认值 | 说明 |
|---|---|---|
| `LOCAL_UPLOAD_DIR` | `./data/uploads` | 文件与 metadata 的保存目录，需要持久化卷 |
| `LOCAL_UPLOAD_BASE_URL` | 空 | 可选的公开 base URL；留空时返回站内相对地址 |

### CloudFront

| 变量 | 说明 |
|---|---|
| `CLOUDFRONT_DOMAIN` | CDN 域名 |
| `CLOUDFRONT_KEY_PAIR_ID` | CloudFront key pair ID |
| `CLOUDFRONT_PRIVATE_KEY` | 完整私钥 |
| `CLOUDFRONT_PRIVATE_KEY_SECRET` | 从 Secrets Manager 读取私钥时使用 |

## Redis 与限流

| 变量 | 默认值 | 说明 |
|---|---|---|
| `REDIS_URL` | 空 | 用于共享限流、实时事件和 token cache；未设置时实时事件和邀请限流回退到进程内存，认证限流不生效 |
| `REDIS_DISABLE_CLIENT_NAME` | `false` | 托管 Redis 禁止 `CLIENT SETNAME` 时设为 `true` |
| `RATE_LIMIT_AUTH` | `5` | 单 IP 每分钟发送验证码或发起 Google 登录的次数 |
| `RATE_LIMIT_AUTH_VERIFY` | `20` | 单 IP 每分钟验证验证码的次数 |
| `RATE_LIMIT_INVITATION_ACTOR_10M` | `10` | 每位邀请人在 10 分钟滑动窗口内可创建的工作区邀请数；设为 `0` 可关闭此项限流 |
| `RATE_LIMIT_INVITATION_WORKSPACE_24H` | `50` | 同一工作区内所有管理员在 24 小时滑动窗口内可创建的邀请总数；设为 `0` 可关闭此项限流 |
| `RATE_LIMIT_INVITATION_RECIPIENT_24H` | `6` | 同一规范化收件邮箱跨工作区在 24 小时滑动窗口内可收到的邀请数；设为 `0` 可关闭此项限流 |
| `RATE_LIMIT_CONTACT_SALES` | `5` | 单 IP 每小时请求 `/contact-sales` 的最大次数 |
| `RATE_LIMIT_PLUGIN_API` | `120` | 单 IP 每分钟请求插件 Public API 的最大次数 |
| `RATE_LIMIT_TRUSTED_PROXIES` | 空 | 允许提供 `X-Forwarded-For` 的代理 CIDR，逗号分隔 |
| `MULTICA_TRUSTED_PROXIES` | 空 | 自动化 webhook 与实时连接使用的可信代理 CIDR |

反向代理后的部署需要填写真实代理网段。不要直接信任所有来源，否则客户端可以伪造转发 IP。

认证限流需要配置 `REDIS_URL`；未配置时，启动日志会提示认证限流已关闭。邀请限流在没有 Redis 时仍使用进程内存，配置 Redis 后则在多个副本间共享额度。如果已配置的 Redis 临时不可用，认证限流仍选择放行，而邀请创建会返回可重试的 `503`，不会在失去防护时发送邮件。

### 实时中继

Redis 也为实时事件（推送到打开的浏览器标签页的任务和对话更新）的扇出提供支持。这部分与 `REDIS_URL` 相互独立，在生产环境中值得与请求路径的缓存分开部署。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `REALTIME_RELAY_REDIS_URL` | 回退到 `REDIS_URL` | 专用于实时流的 Redis/Valkey；要求支持 XTRIM MINID（Redis 6.2+） |
| `REALTIME_RELAY_MODE` | `sharded` | `sharded`、`dual`（双写、按分片读取——用于迁移过渡）或 `legacy`（单一 stream）；无法识别的值会回退到 `sharded` 并给出警告 |
| `REALTIME_RELAY_SHARDS` | `8` | 分片中继的分片数量；无效值或 0 会回退到默认值 |
| `REALTIME_RELAY_XREAD_COUNT` | `128` | 每次轮询各分片 stream 时的 `XREAD COUNT` |
| `REALTIME_RELAY_XREAD_BLOCK` | `5s` | 每次轮询各分片 stream 时的 `XREAD BLOCK` 时长 |
| `REALTIME_RELAY_STREAM_MAXLEN` | `2000` | 每个分片保留的条目数；请结合观测到的单条大小、事件速率和 Redis 内存预算来设定 |
| `REALTIME_RELAY_REPLAY_GRACE` | `5m` | 分片读取端的重放宽限期 |
| `REALTIME_RELAY_TRIM_HORIZON` | `10m` | 精确按时间裁剪的时间线，在 `REALTIME_RELAY_STREAM_MAXLEN` 之后应用 |
| `REALTIME_RELAY_STREAM_TTL_ENABLED` | `false` | 一项需要主动开启的分阶段兜底方案，会在过期时清空整个 stream；开启前请先查看 `.env.example` 中的上线/回滚步骤 |
| `REALTIME_RELAY_STREAM_TTL` | `15m` | stream 的 TTL，只有在 `REALTIME_RELAY_STREAM_TTL_ENABLED=true` 时才生效 |
| `REALTIME_RELAY_TTL_REFRESH_INTERVAL` | `30s` | 活跃 stream 刷新 TTL 的频率 |
| `REALTIME_RELAY_MAINTENANCE_INTERVAL` | `1m` | 后台维护清理的执行间隔 |

### 频道 WebSocket 租约

多副本部署需要基于 Redis 的租约，确保同一时间只有一个 API 副本持有某个频道的入站 WebSocket 连接（企业微信等长连接的频道）。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `CHANNEL_WS_LEASE_BACKEND` | PostgreSQL | 多副本生产环境请设为 `redis`；启动时如果 Redis、`Ping` 或原子租约脚本不可用，supervisor 会直接拒绝启动。要切换就所有副本一起切——没有双锁模式 |
| `CHANNEL_WS_LEASE_REDIS_URL` | 回退到 `REDIS_URL` | 强烈建议使用一个专用于租约的 Redis/Valkey 实例，并配置 `maxmemory-policy noeviction` |
| `CHANNEL_WS_LEASE_NAMESPACE` | 无（Redis 模式下必填） | 稳定的部署标识符，避免共享同一个 Redis 数据库的多个环境互相争抢对方的安装 ID；只能使用字母、数字、点、下划线和连字符 |
| `CHANNEL_WS_LEASE_TTL` | `180s` | 租约 TTL；时长需满足 `0 < poll <= renew < TTL` |
| `CHANNEL_WS_LEASE_RENEW_INTERVAL` | `60s` | 租约续期间隔 |
| `CHANNEL_WS_LEASE_POLL_INTERVAL` | `30s` | 获取租约的轮询间隔 |
| `CHANNEL_WS_LEASE_ERROR_RETRY_INTERVAL` | `5s` | Redis 出错后的重试间隔 |
| `CHANNEL_WS_LEASE_EXPIRY_SAFETY_MARGIN` | `5s` | 持有者会在最后一次确认的 TTL 到期前这么久主动断开，以防止脑裂 |

租约 key 被静默驱逐会中断频道连接——请对该租约实例的内存/容量、驱逐次数（必须保持为零）、可用性和命令延迟设置告警。

## 外部集成

| 集成 | 变量 | 说明 |
|---|---|---|
| GitHub | `GITHUB_APP_SLUG` | GitHub App slug |
| GitHub | `GITHUB_WEBHOOK_SECRET` | Webhook HMAC 与连接 state 签名密钥 |
| GitHub | `GITHUB_APP_ID` | PR 卡片的 CI 状态、可合并性和"从 GitHub 选择"仓库都需要它 |
| GitHub | `GITHUB_APP_PRIVATE_KEY` | 与 App ID 配套的完整 PEM 私钥，用途同上 |
| GitHub | `GITHUB_TOKEN` | 从 GitHub URL 导入 skill 时，未认证的 `api.github.com` 请求所附带的 Bearer 个人访问令牌；把单 IP 限流从 60/小时提升到 5000/小时。与上面的 GitHub App 无关 |
| 飞书 | `MULTICA_LARK_SECRET_KEY` | base64 编码的 32 字节凭据加密密钥 |
| 飞书 | `MULTICA_LARK_HTTP_BASE_URL` / `MULTICA_LARK_CALLBACK_BASE_URL` | 部署级覆盖，强制所有安装实例使用同一个飞书/Lark host（代理、测试 mock 或单云 staging 环境）；正常使用请两者都留空——区域会按每个安装实例自动检测 |
| 飞书 | `MULTICA_LARK_WS_PROXY_URL` | WebSocket 长连接握手可选的固定 HTTP CONNECT 代理；留空则使用标准的 `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY` |
| Slack | `MULTICA_SLACK_SECRET_KEY` | base64 编码的 32 字节 token 加密密钥 |
| Telegram | `MULTICA_TELEGRAM_SECRET_KEY` | base64 编码的 32 字节 Bot token 加密密钥 |
| 钉钉 | `MULTICA_DINGTALK_SECRET_KEY` | base64 编码的 32 字节 AppSecret 加密密钥 |
| 企业微信 | `MULTICA_WECOM_SECRET_KEY` | base64 编码的 32 字节密钥，用于加密每个安装实例的智能机器人 secret；留空会禁用该集成及其 Web UI 接口（503） |
| 企业微信 | `MULTICA_WECOM_MEDIA_ALLOW_CIDRS` | 逗号分隔的网段，媒体抓取器即使识别为保留地址也允许拨号；除非部署在伪 IP 代理之后，否则留空 |
| 企业微信 | `MULTICA_WECOM_TRACE` | 设为 `1` 会记录企业微信适配器读写的每一帧，包括消息内容（前 120 个字符）；仅用于调试，会话结束后请取消设置 |
| Composio | `COMPOSIO_API_KEY` | 启用 Composio 工具连接 |
| Composio | `COMPOSIO_CALLBACK_BASE_URL` | 回调 API 地址；可回退到 `MULTICA_PUBLIC_URL` |
| Composio | `COMPOSIO_STATE_SECRET` | OAuth state 签名密钥；可从 `JWT_SECRET` 派生 |
| 自托管 Git | `MULTICA_VCS_INTEGRATION_ENABLED` | Forgejo/Gitea/GitLab 集成开关，compose 默认开启 |
| 自托管 Git | `MULTICA_VCS_SECRET_KEY` | base64 编码的 32 字节加密密钥（`openssl rand -base64 32`）；未配置时该功能整体不可用 |
| Linear Bridge | `MULTICA_LINEAR_SECRET_KEY` | base64 编码的 32 字节密钥，用于加密每个工作区的 Linear OAuth token 和 webhook secret；未设置会整体禁用该集成 |
| Linear Bridge | `MULTICA_LINEAR_CLIENT_ID` / `MULTICA_LINEAR_CLIENT_SECRET` | 来自 `linear.app/settings/api/applications` 的 OAuth 应用凭据；未设置时已有安装实例仍会同步，但会阻止新增安装 |
| Linear Bridge | `MULTICA_LINEAR_STATE_SECRET` | 可选的 OAuth state 签名密钥；未设置时用从 `JWT_SECRET` 派生的密钥签名 state |
| 插件 | `MULTICA_PLUGIN_SECRET_KEY` | base64 编码的 32 字节密钥，用于加密已存 secret 和 surface 启动 URL |
| 插件 | `MULTICA_PLUGIN_SURFACE_ORIGIN` | 路由到后端的独立无 Cookie 浏览器 origin；必须不同于 app/API origin，并保留 `Host` |
| 插件 | `MULTICA_PLUGIN_API_URL` | 带版本的插件 Public API 完整 Base URL，例如 `https://plugin-api.example.com/v1`；未设置时使用 `MULTICA_PUBLIC_URL` + `/v1` |
| 插件 | `MULTICA_PLUGIN_DIR` | 开发时用于发布本地插件包的可选绝对目录 |
| 模型密钥（BYOK） | `MULTICA_MODEL_KEY_SECRET_KEY` | base64 编码的 32 字节密钥，用于加密工作区和项目在「设置 → 模型密钥」中填写的服务商 API key；未设置时显示为"未配置"，并拒绝保存密钥 |
| SSO | `MULTICA_SSO_SECRET_KEY` | base64 编码的 32 字节密钥，用于加密每个工作区的 OIDC client secret；未设置时"安全"标签页显示为"未配置" |

不配置 `GITHUB_APP_ID` 和私钥时，PR 仍会正常关联、镜像和触发合并转 done，但卡片上不显示 CI 与可合并状态，"从 GitHub 选择"仓库入口也会禁用。

配置步骤见 [GitHub 集成](/github-integration#自托管配置)、[飞书 Bot](/lark-bot-integration#自托管配置)、[Slack Bot](/slack-bot-integration#自托管配置)和 [Telegram Bot](/telegram-bot-integration#自托管配置)。

## 服务端 LLM

这组配置用于服务端的辅助生成能力，例如对话标题；它不是智能体运行时使用的 AI 编程工具凭据。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_LLM_API_KEY` | 空 | OpenAI 兼容 API key |
| `MULTICA_LLM_BASE_URL` | 空 | OpenAI 兼容 endpoint |
| `MULTICA_LLM_DEFAULT_MODEL` | `gpt-5.6-luna` | 请求未指定模型时使用 |
| `MULTICA_LLM_ROUTING_MODEL` | 回退到 `MULTICA_LLM_DEFAULT_MODEL` | 用于判断一次 webhook 投递是否符合自动化自然语言路由规则的小型快速模型 |
| `MULTICA_LLM_EMBEDDING_MODEL` | 空 | 为共享仓库索引启用语义排序。模型必须产生 1536 维向量；留空则索引只按关键词工作 |
| `MULTICA_LLM_MAX_RETRIES` | `2` | 单次调用的重试上限；`0` 表示禁用重试，`1`–`5` 表示最多重试 N 次 |

`MULTICA_LLM_MAX_RETRIES` 是重试策略的唯一配置源。不设置时使用默认的 2 次；设为 `0` 表示每次调用只发一个请求；设为 1–5 表示最多重试这么多次。它是上限而不是配额：只有可重试的失败才会消耗它，请求成功或调用方自身的超时都会让调用提前结束。其它取值（负数、非数字、大于 5）会让服务启动失败，而不是被静默纠正。上限是一个延迟预算：退避从 0.5s 开始翻倍，上限 8s，更大的预算会超出调用方自身的超时，把可重试的失败变成超时失败。重试覆盖连接失败以及 HTTP 408、409、429 和 5xx；其余 4xx 直接返回。服务启动时会以 `llm retry policy` 打印生效策略，其中不包含任何凭据。

以下功能会使用这一层，它们都会把内容发送到你配置的 endpoint：

- **对话标题自动生成** —— 发送新对话中用户的第一条消息，原文发送。附件不会包含在内。
- **后续提问建议**（智能体回复下方的按钮）—— 发送对话末尾的内容：最多 6 条消息，其中被追问的那条回复上限 3000 字符，更早的每条上限 800 字符。
- **共享语义仓库索引** —— 发送工作区显式开启的仓库（设置 → 代码仓库 → 语义索引）的代码片段正文，以及作为检索词的任务标题和描述前 500 个字符。只有设置了 `MULTICA_LLM_EMBEDDING_MODEL` 才会启用；留空时索引仍按关键词排序正常工作，仓库代码不会离开你的部署。
- **Issue scoping assistant**（创建任务对话框中的"用几句话生成草稿"）—— 发送成员输入的文本，上限 8000 字符，以及目标项目的标题和描述。
- **[原生运行时](/native-runtime)** —— 以无守护进程方式运行的智能体，其工具调用循环会把任务和评论内容发送给同一个客户端。这里不使用 `MULTICA_LLM_ROUTING_MODEL`；原生运行时调用使用的是 `MULTICA_LLM_DEFAULT_MODEL`。

API key 与 base URL 都为空时，这一层关闭，不会发出任何上游请求——上面两个功能都不再发送内容。如果你的政策不允许这一层把聊天内容发到部署之外，这就是受支持的配置方式：对话继续使用客户端根据第一条消息生成的标题，后续提问按钮不再出现，其余功能不受影响。

<Callout type="info">
这只覆盖辅助生成这一层。智能体执行是另一条数据路径：智能体回复对话时，由你的守护进程调用该智能体的 AI 编程工具，用的是那个工具自己的凭据，守护进程不会把上面的 `MULTICA_LLM_*` 配置传给它。（智能体自身需要的运行级 Multica 连接变量由守护进程单独注入。）把上面的变量留空不影响这条路径——它由智能体的运行时配置决定。
</Callout>

## 语音：语音转文字与文字转语音

两者都是可选功能，且各自独立开关；不设置对应服务商时，相关功能会被隐藏而不是报错。

### 语音转文字（语音备忘录、会议转录）

任何实现了 OpenAI 兼容的 `POST {base}/v1/audio/transcriptions` 接口的服务商均可：Mistral Voxtral、OpenAI、Groq，或部署在 vLLM 之后自托管的 Qwen3-ASR / Whisper。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_STT_BASE_URL` | 空 | STT endpoint；留空此项或模型会禁用 STT——相关接口返回 `409`，UI 隐藏该功能 |
| `MULTICA_STT_API_KEY` | 空 | STT 服务商 API key |
| `MULTICA_STT_MODEL` | 空 | STT 模型名称 |
| `MULTICA_STT_LANGUAGE` | 服务商自动检测 | 以 `language` 字段发送的 ISO 639-1 语言提示（例如 `fr`） |
| `MULTICA_STT_DIARIZE` | `false` | 设为 `true` 会要求支持该功能的服务商（Voxtral）标注说话人 |
| `MULTICA_STT_REALTIME_MODEL` | 空 | 启用会议录制中的实时逐词转录；浏览器会用服务端签发的短期 token 打开服务商的实时 WebSocket |

音频转录完成后即被丢弃——不会被保存。

### 文字转语音（智能体消息的"朗读"功能）

任何实现了 OpenAI 兼容的 `POST {base}/v1/audio/speech` 接口的服务商均可：OpenAI、Groq，或自托管的 Kokoro-FastAPI / openedai-speech。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_TTS_BASE_URL` | 空 | TTS endpoint；留空此项或模型会禁用 TTS——`/api/voice/speak` 返回 `409`，客户端会回退到浏览器自带的 `speechSynthesis` |
| `MULTICA_TTS_API_KEY` | 空 | TTS 服务商 API key |
| `MULTICA_TTS_MODEL` | 空 | TTS 模型名称 |
| `MULTICA_TTS_VOICE` | 服务商默认值 | 服务商的语音 id（OpenAI：`alloy`、`nova` 等）；留空则由服务商自行选择，部分服务商会拒绝这种情况 |

音频按请求实时合成，不会被保存。

## 移动推送

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_PUSH_DISABLED` | 空 | 设为 `1` 可完全禁止发送推送通知 |
| `MULTICA_EXPO_PUSH_ENDPOINT` | Expo 的默认值（`https://exp.host/--/api/v2/push/send`） | 可选，用于覆盖 Expo 推送 API 的 endpoint |

Expo 推送不需要任何凭据即可工作。

## 功能开关

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_FEATURE_FLAGS_FILE` | 空 | 声明功能开关规则的 YAML 文件路径；未设置时每个开关都会回退到调用方自己的默认值，因此在编写任何开关配置之前服务器也能正常启动。已设置的文件如果解析或 IO 出错，会导致启动失败，与 `DATABASE_URL` 或 `JWT_SECRET` 配置错误的处理方式相同 |

单个开关也可以通过 `FF_<FLAG_KEY>` 环境变量覆盖，而不必改动 YAML（例如 `FF_BILLING_NEW_INVOICE_EMAIL=false`、一个百分比，或任意变体字符串）。环境变量覆盖优先于 YAML——这是运维层面的一键开关路径：设置好环境变量后重启进程，无需重新部署即可翻转某个开关。完整的 YAML schema 见 `server/pkg/featureflag`。

## 自动化失败监控

定期暂停最近运行记录以失败为主的自动化，避免配置错误的定时自动化在死循环中无限消耗运行次数和 token。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `AUTOPILOT_FAIL_MONITOR_INTERVAL` | `24h` | 扫描的执行频率；设为 `0` 会完全禁用该监控 |
| `AUTOPILOT_FAIL_MONITOR_LOOKBACK` | `168h` | 扫描统计运行次数时回溯的时间范围 |
| `AUTOPILOT_FAIL_MONITOR_STARTUP_DELAY` | `1m` | 进程启动后到第一次扫描之间的延迟 |
| `AUTOPILOT_FAIL_MONITOR_MIN_RUNS` | `50` | 回溯窗口内至少需要多少次运行，失败率才会被认为在统计上有意义；必须是正整数 |
| `AUTOPILOT_FAIL_MONITOR_FAIL_RATIO` | `0.9` | 触发自动暂停的失败率区间 (0, 1] |

## 自托管部署

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_CLOUD_URL` | 空 | 连接到托管的 multica-cloud 服务；自托管时留空。连接后，Fleet、账单、权益策略和预购席位容量都会使用这项服务 |
| `MULTICA_IMAGE_TAG` | `latest` | 自托管发布渠道；固定到某个具体版本（例如 `v0.2.4`）可以锁定在该版本上 |
| `MULTICA_BACKEND_IMAGE` | `ghcr.io/multica-ai/multica-backend` | 后端镜像仓库 |
| `MULTICA_WEB_IMAGE` | `ghcr.io/multica-ai/multica-web` | Web 镜像仓库 |

## 守护进程配置

下面的变量在运行智能体的电脑上读取，不是在 API 容器中读取。

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MULTICA_SERVER_URL` | `ws://localhost:8080/ws` | Multica API / WebSocket 地址，也接受 `http(s)` |
| `MULTICA_DAEMON_DEVICE_NAME` | 主机名 | 运行时列表中的设备名 |
| `MULTICA_AGENT_RUNTIME_NAME` | `Local Agent` | 运行时显示名 |
| `MULTICA_DAEMON_POLL_INTERVAL` | `30s` | 没有唤醒事件时的运行轮询间隔 |
| `MULTICA_DAEMON_WS_CLAIM_POLL_INTERVAL` | `3m` | 健康 WebSocket 下 claim 安全轮询的上限，与 `MULTICA_DAEMON_POLL_INTERVAL` 独立配置；向下抖动后默认实际为 `2m30s`–`2m45s`，旧 Server 或结果不确定的 claim 仍使用普通轮询间隔 |
| `MULTICA_DAEMON_HEARTBEAT_INTERVAL` | `15s` | 心跳间隔 |
| `MULTICA_DAEMON_MAX_CONCURRENT_TASKS` | `20` | 单个守护进程的并发运行上限 |
| `MULTICA_AGENT_TIMEOUT` | `0` | 单次执行的绝对时限；`0` 表示不设置 |
| `MULTICA_AGENT_IDLE_WATCHDOG` | `2h` | 没有输出且没有工具执行时的静默上限；`0` 表示整套 watchdog 关闭 |
| `MULTICA_AGENT_TOOL_WATCHDOG` | 同 `MULTICA_AGENT_IDLE_WATCHDOG` | 单个工具调用持续静默的上限；只有希望工具比模型有更多余量时才单独设置，`0` 表示工具执行期间永不强制停止 |
| `MULTICA_OPENCODE_IDLE_WATCHDOG` | `10m` | OpenCode 专用静默阈值 |
| `MULTICA_CODEX_SEMANTIC_INACTIVITY_TIMEOUT` | 同 `MULTICA_AGENT_IDLE_WATCHDOG` | Codex 语义静默阈值。Codex 自己的计时器看不到工具是否在执行，因此跟随 idle / tool 预算中较大的那个，而不再单独保留一个更短的上限 |
| `MULTICA_CODEX_FIRST_TURN_TIMEOUT` | `0` | 显式覆盖 Codex 首轮无进展上限；`0` 保持默认值。实际首轮等待仍受 `MULTICA_CODEX_SEMANTIC_INACTIVITY_TIMEOUT` 和总执行超时限制——需将 `MULTICA_CODEX_SEMANTIC_INACTIVITY_TIMEOUT` 设为严格大于此值（并留出余量），否则等待会被截断至该值，且会跳过模型目录启动重试。设为相等还不够：语义计时器先启动，两者相等时仍可能丢失该重试 |
| `MULTICA_CODEX_HANDSHAKE_TIMEOUT` | `30s`；`thread/start`、`thread/resume`：`60s` | Codex app-server 启动握手上限；显式设置后会统一覆盖两类预算 |
| `MULTICA_DAEMON_AUTO_UPDATE` | Cloud `true`；自托管 `false` | 是否自动检查并更新 CLI |
| `MULTICA_DAEMON_AUTO_UPDATE_INTERVAL` | `6h` | 检查更新的间隔 |
| `MULTICA_DAEMON_AUTO_RELOAD` | `true` | 是否在 `multica` 二进制被带外替换（`brew upgrade`、重新下载、本地构建）后重启到新二进制。与 `MULTICA_DAEMON_AUTO_UPDATE` 相互独立 |
| `MULTICA_WORKSPACES_ROOT` | `~/multica_workspaces` | 执行工作目录的根目录 |
| `MULTICA_AGENT_TEMP_BASE` | `/tmp`（Linux/macOS） | 仅适用于 Linux/macOS。每次运行的私有临时目录的父目录；必须是现有、可写的绝对路径，无效值会使运行启动失败，不会回退到 `/tmp`。请选择较短的路径——子工具可能会在其中绑定 AF_UNIX socket；Linux 的 `sun_path` 上限为 108 字节，macOS 为 104 字节 |
| `MULTICA_KEEP_ENV_AFTER_TASK` | `false` | 保留运行目录用于调试 |

守护进程注入智能体 task 的内部上下文见 [Task 运行时环境](/daemon-runtimes#task-运行时环境)。

各 AI 编程工具可以使用 `MULTICA_<PROVIDER>_PATH` 覆盖命令路径；支持模型覆盖的工具还可以使用 `MULTICA_<PROVIDER>_MODEL`。QwenPaw 和 MiniMax Code 没有模型变量，因为 Multica 不会向它们传模型；MiniMax Code 的路径变量是 `MULTICA_MCODE_PATH`。详见 [AI 编程工具对比](/providers#模型来源)。DeepSeek Harness 支持 `MULTICA_DSH_PATH` 和 `MULTICA_DSH_MODEL`（取值为 dsh 模型目录中的模型 id，例如 `deepseek-official/deepseek-chat`）。ZeroClaw 支持 `MULTICA_ZEROCLAW_PATH`，但没有模型变量；模型由 ZeroClaw 的智能体配置管理。全机默认参数 `MULTICA_<PROVIDER>_ARGS` 目前支持 Claude Code、Codex、CodeBuddy、Qwen Code 和 QwenPaw 五款工具。对应变量是 `MULTICA_CLAUDE_ARGS`、`MULTICA_CODEX_ARGS`、`MULTICA_CODEBUDDY_ARGS`、`MULTICA_QWEN_ARGS` 和 `MULTICA_QWENPAW_ARGS`。例如：

```dotenv
MULTICA_CLAUDE_PATH=/opt/bin/claude
MULTICA_CLAUDE_ARGS=--max-turns 40
```

优先级为命令行 flag → 环境变量 → `~/.multica/config.json` → 内置默认。watchdog 的行为见[守护进程与运行时](/daemon-runtimes)。

### 守护进程配置的持久化

守护进程侧的常用配置也可以写入 `~/.multica/config.json`，不必依赖 shell 环境变量；命名 profile 的配置文件在 `~/.multica/profiles/<name>/config.json`：

```bash
multica config set poll_interval 10s
multica config show
```

支持的键：

| 键 | 默认值 | 说明 |
|---|---|---|
| `server_url` | `ws://localhost:8080/ws` | Multica API / WebSocket 地址 |
| `app_url` | 空 | 浏览器登录使用的 Web 地址 |
| `workspace_id` | 空 | 默认工作区 |
| `device_name` | 主机名 | 运行时列表中的设备名 |
| `runtime_name` | `Local Agent` | 运行时显示名 |
| `workspaces_root` | `~` 下按 profile 区分的路径 | 执行工作目录的根目录 |
| `max_concurrent_tasks` | `20` | 并发运行上限；`0` 或留空表示未设置 |
| `poll_interval` | `30s` | 运行轮询间隔 |
| `ws_claim_poll_interval` | `3m` | 健康 WebSocket 下 claim 安全轮询的上限，与 `poll_interval` 独立配置；Daemon 会应用向下抖动 |
| `heartbeat_interval` | `15s` | 心跳间隔 |
| `agent_timeout` | 不限制 | 单次执行的绝对时限 |
| `codex_semantic_inactivity_timeout` | 派生 | Codex 语义静默阈值。未设置时取 idle 与 tool watchdog 预算中较大的那个；tool 预算为 `0` 时回落到 idle 预算；只有整套 watchdog 关闭时才保留 Codex 自己的 `10m` |
| `codex_handshake_timeout` | `30s`；`thread/start`、`thread/resume`：`60s` | Codex app-server 启动握手上限；显式设置后会统一覆盖两类预算 |
| `disable_auto_update` | 跟随环境 | `true` 关闭自动更新；`false` 清除本地覆盖，回到环境变量或默认 |
| `auto_update_check_interval` | `6h` | 检查更新的间隔 |
| `disable_auto_reload` | 跟随环境 | `true` 让守护进程不再跟随磁盘上被替换的二进制；`false` 清除本地覆盖。与 `disable_auto_update` 分开解析 |

几条取值规则：

- duration 类键接受正的 Go duration（如 `10s`、`2h`），`0s` 和负值会被拒绝。唯一例外是 `agent_timeout`：`0s` 合法，表示明确关闭执行时限。
- 传空字符串清除已持久化的值，回到环境变量或内置默认，例如 `multica config set poll_interval ""`。
- `max_concurrent_tasks` 要求非负整数。
- 相对的 `workspaces_root` 值在保存时会转换为绝对路径。

## 观测与统计

| 变量 | 默认值 | 说明 |
|---|---|---|
| `ANALYTICS_DISABLED` | `false` | 设为 `true` 关闭 PostHog 上报 |
| `POSTHOG_API_KEY` | 空 | 不设置时统计上报关闭；接入自己的 PostHog 项目时填写 |
| `POSTHOG_HOST` | `https://us.i.posthog.com` | PostHog 地址 |
| `METRICS_ADDR` | 空 | Prometheus metrics 监听地址；空表示不启动 |
| `REALTIME_METRICS_TOKEN` | 空 | 保护 `/health/realtime` 的 bearer token |

## 接下来

- [登录与注册](/auth-setup) — 登录方式与注册限制。
- [故障排查](/troubleshooting) — 部署问题的症状排查。
- [守护进程与运行时](/daemon-runtimes) — 守护进程侧的对应配置。
