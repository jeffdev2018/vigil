---
title: 에이전트
description: 에이전트는 워크스페이스에서 반복해서 사용하는 정체성, 기능, 실행 설정의 묶음입니다.
---

import { Callout } from "fumadocs-ui/components/callout";

에이전트는 [Multica 워크스페이스](/workspaces)의 협업자입니다. 태스크를 할당하거나 댓글에서 @멘션하거나 직접 대화할 수 있습니다. 에이전트는 연결된 런타임을 통해 AI 코딩 도구를 호출하고 진행 상황과 결과를 워크스페이스에 기록합니다.

에이전트 자체는 계속 실행되는 프로세스가 아닙니다. 반복해서 사용할 수 있는 정체성과 설정의 묶음이며, 작업을 받을 때만 구체적인 [실행](/tasks)이 생성됩니다.

## 에이전트 설정

| 설정 | 역할 |
|---|---|
| **이름, 아바타, 설명** | 팀에 에이전트가 누구이며 어떤 작업에 적합한지 알려 줍니다. 설명은 표시용이며 실행 프롬프트에 포함되지 않습니다. |
| **지시문** | 역할, 작업 방식, 경계, 결과물 요구 사항을 규정합니다. 모든 실행에서 사용됩니다. |
| **대화 시작 예시** | 에이전트별 예시를 최대 3개 제공하며, 새 채팅을 열 때 표시됩니다. 선택하면 전송하지 않고 입력창에 채웁니다. |
| **스킬** | 재사용 가능한 방법, 참고 자료, 지원 파일을 제공합니다. |
| **런타임, 모델, 사고 강도** | 어느 런타임에서 어떤 AI 코딩 도구와 모델을 사용할지 결정합니다. Codex 같은 일부 도구에서는 속도 단계도 선택할 수 있습니다. |
| **Access** | 어떤 멤버가 에이전트를 실행할 수 있는지 결정합니다. |
| **실행 설정** | 동시 실행 상한, 환경 변수, CLI 인수, MCP, 외부 연동을 포함합니다. |

모델을 바꾸거나 지시문을 수정해도 새 에이전트가 만들어지지 않으며 이전 태스크, 댓글, 실행 기록도 사라지지 않습니다.

## 에이전트, 런타임, 실행

| 개념 | 역할 |
|---|---|
| **에이전트** | 누가 어떤 방식으로 작업할지 결정 |
| **런타임** | 어느 컴퓨터에서 어떤 AI 코딩 도구로 실행할지 결정 |
| **실행** | 이번 실행의 과정과 결과를 기록 |

하나의 런타임이 여러 에이전트를 지원할 수 있고, 하나의 에이전트도 여러 실행을 차례로 완료할 수 있습니다. 런타임이 오프라인이어도 에이전트의 정체성과 기록은 남아 있지만 새 실행은 런타임이 복구될 때까지 기다려야 합니다.

## 작업에 참여하는 방법

- **태스크 담당**: 에이전트를 담당자로 지정하면 실행이 큐에 들어간 뒤 처리를 시작합니다.
- **댓글 하나 처리**: 댓글에서 에이전트를 @멘션하며 태스크 담당자는 바뀌지 않습니다.
- **논의 이어가기**: 에이전트의 댓글에 답하면 새 정보가 이후 실행에 들어갑니다.
- **직접 대화**: 태스크에 연결되지 않은 대화를 시작합니다.
- **프로젝트와 스쿼드 참여**: 에이전트는 프로젝트 lead가 되거나 스쿼드 리더 또는 멤버가 될 수 있습니다.
- **오토파일럿을 통한 실행**: 일정이나 외부 이벤트로 미리 설정한 작업을 트리거하거나 수동으로 실행합니다.

에이전트는 태스크를 만들고, 댓글을 작성하고, 작업 상태를 변경할 수 있지만 인박스가 없으며 `@all` 알림도 받지 않습니다. 에이전트 @멘션은 알림이 아니라 실행 트리거입니다.

## 권한과 Access

각 에이전트에는 owner 한 명과 Access 설정 하나가 있습니다.

| Access | 실행할 수 있는 사람 |
|---|---|
| **나만**(Only me) | 에이전트 owner만 실행할 수 있습니다. |
| **전체 워크스페이스**(Entire workspace) | 워크스페이스의 모든 멤버가 실행할 수 있습니다. |
| **지정한 멤버**(Specific people) | 에이전트 owner와 지정된 멤버가 실행할 수 있습니다. |

새 에이전트의 기본값은 **나만**입니다.

<Callout type="info">
워크스페이스 `owner`와 `admin`은 모든 에이전트를 보고 관리할 수 있지만 다른 사람의 "나만" 에이전트를 Access를 우회해 실행할 수는 없습니다. 일반 멤버에게는 자신이 소유하거나 실행할 권한이 있는 에이전트만 표시됩니다.
</Callout>

**보기 권한**은 owner 여부, 워크스페이스 관리 역할, Access가 함께 결정합니다. **수정 권한**은 에이전트 owner와 워크스페이스 owner/admin에게 있으며, 이 중 Access는 에이전트 owner만 바꿀 수 있습니다.

환경 변수와 MCP처럼 자격 증명이 포함될 수 있는 설정에는 더 엄격한 읽기 규칙이 적용됩니다. [에이전트 생성 및 설정](/agents-create#환경-변수와-자격-증명)을 참고하세요.

## 온라인, 오프라인, 작업 상태

에이전트 목록에는 두 종류의 상태가 함께 표시됩니다.

- **가용성**은 런타임에서 오며 온라인, 오프라인, 불안정 등이 있습니다.
- **작업량**은 실행에서 오며 처리 중, 대기 중, 유휴 등이 있습니다.

"오프라인"은 에이전트가 삭제되었다는 뜻이 아닙니다. 이미 큐에 들어간 실행은 런타임이 복구될 때까지 기다립니다. "보관됨" 상태에서만 새 작업을 받을 수 없습니다.

## 보관 및 복원

더 이상 사용하지 않는 에이전트는 보관할 수 있습니다. 보관 후에는 선택기에 표시되지 않고 할당하거나 @멘션할 수도 없습니다. 이전 기록은 유지되며 나중에 복원할 수 있습니다.

<Callout type="warning">
보관하면 큐 대기 중이거나 진행 중인 실행을 포함해 해당 에이전트의 끝나지 않은 모든 실행이 취소됩니다.
</Callout>

에이전트 owner와 워크스페이스 `owner`, `admin`은 모두 에이전트를 보관하거나 복원할 수 있습니다.

## 메모리와 교정

**메모리** 탭은 에이전트의 짧은 규칙을 저장합니다. 새 실행에는 생성일이 가장 최근인 활성 상태의 미만료 메모리 최대 50개가 포함됩니다. 이미 시작한 실행은 변경되지 않습니다.

편집 화면에서 현지 시간으로 만료일을 선택할 수 있습니다. 만료된 메모리는 계속 표시되지만 새 실행에는 포함되지 않습니다. 내용을 수정하거나 승인해도 만료일은 유지됩니다. 다시 사용하려면 날짜를 명시적으로 변경하거나 지워야 합니다. 만료일 변경만으로 검토 대기 제안이 승인되지는 않습니다. 만료 항목도 200개 상한에 포함됩니다.

직접 추가한 메모리는 즉시 활성화되며 항목당 최대 500자입니다. 완료된 실행에서 자동 추출한 제안은 사람의 검토를 기다립니다. 비공개 채팅은 추출에서 제외됩니다. 활동 기록에서 완료, 실패 또는 취소된 실행에 교정을 연결하고 메모리 탭에서 출처 확인, 수정, 승인 또는 거절할 수 있습니다. 수정만으로 승인되지는 않습니다.

**기록**에서 버전을 확인하고 복원을 미리 볼 수 있습니다. 복원하면 선택한 내용, 검토 상태와 만료일을 가진 새 버전이 생성되며 복원한 사람을 기록합니다. 활성 상태의 미만료 버전은 새 실행에 사용될 수 있고, 검토 대기 버전은 여전히 승인이 필요합니다. 동시 수정이 발생하면 기록으로 돌아가 최신 상태를 확인한 뒤 다시 시도하세요. 기록은 업그레이드 시 보존한 상태부터 시작하며 이전 수정은 재구성할 수 없습니다. 메모리를 삭제하면 기록도 영구 삭제됩니다.

에이전트 소유자와 워크스페이스 소유자·관리자만 메모리를 변경할 수 있으며 머신 자격 증명으로는 승인할 수 없습니다. 검토자와 시간을 기록하고 동시 수정 시 새로고침을 요구합니다. 사용 중지는 기록을 보존하며, 다시 검토하면 검토 대기로 돌아갑니다. 삭제는 영구적입니다.

대기 및 거절된 항목을 포함해 에이전트당 최대 200개를 보관합니다. 상한에 도달하면 추출을 멈추며 기존 메모리를 자동 삭제하지 않습니다. 업그레이드 시 기존 자동 추출 메모리는 검토 대기로 바뀌고 수동 메모리는 활성 상태를 유지합니다. 승인은 향후 사용을 허용할 뿐 성능 향상을 입증하지 않습니다.

**평가**에서 사람 관리자는 오프라인 비교 JSON 보고서를 가져오고(최대 2 MiB, 메모리당 10개), 후보 유무 및 별도 검증 사례의 결과를 검토하고, 보고서를 내보내거나 삭제하고, 근거 확인 후 적격 리비전을 채택할 수 있습니다. CLI에는 `multica agent memory evaluate`, `publish`, `adopt`가 있습니다. 채택은 전체 활성 메모리 맥락을 같은 트랜잭션에서 검사하고 새 리비전을 기록합니다. 맥락이 바뀌면 다시 평가해야 합니다. 보고서는 사람이 제출한 근거이며 서버가 증명한 실행이나 운영 성능 개선의 증거는 아닙니다. 비용과 사람의 작업량은 미측정입니다. 보고서 삭제는 채택을 취소하지 않습니다. 참조된 메모리를 삭제하면 기준 맥락의 복사본을 포함해 그 텍스트가 들어 있는 보고서도 삭제됩니다. 이전 버전은 메모리 기록에서 복원할 수 있습니다. 웹과 데스크톱에서 지원합니다.



실행 상세에서 메모리 정보를 펼치면 마지막 전달 준비 시 선택한 에이전트 메모리 ID와 버전, 프로젝트 메모리 버전을 확인할 수 있습니다. 불러오기 실패와 대상 메모리가 없는 경우를 구분하며 이전 실행에는 미기록으로 표시합니다. 시작 전에 다시 가져오면 스냅샷이 교체됩니다. 모든 전달의 이력이나 수신·준수의 증거는 아닙니다. 실행 기록에는 버전 참조만 남고 삭제한 메모리 본문은 남지 않습니다.

**실행별 메모리 통계**는 최근 30일간 시작되었고 기록이 남아 있으며 채팅이 아닌 출처를 확인할 수 있는 실행을 집계합니다. 삭제된 채팅과 출처를 알 수 없는 이전 실행은 제외합니다. 메모리 포함, 대상 메모리 없음, 불러오기 실패, 컨텍스트 미기록을 구분합니다. 버전 목록에서 각 버전이 준비된 횟수와 마지막 실행 시작 시각을 확인할 수 있습니다. 현재 본문은 별도로 표시하며 이력에서 보존된 버전을 열 수 있습니다. 통계는 1분마다 갱신됩니다. 모델의 준수나 개선을 측정하지 않으며 요청 실패 시 0 대신 오류를 표시합니다.

## 다음 단계

- [에이전트 생성 및 설정](/agents-create) — 빈 상태에서 시작하거나 AI를 사용해 만듭니다.
- [스킬](/skills) — 에이전트에 재사용 가능한 방법과 자료를 추가합니다.
- [스쿼드](/squads) — 리더가 작업에 맞는 멤버를 선택하게 합니다.
- [데몬과 런타임](/daemon-runtimes) — 에이전트가 실제로 어디에서 실행되는지 알아봅니다.
