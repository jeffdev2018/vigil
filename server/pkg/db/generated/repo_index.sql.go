// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: repo_index.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteRepoIndexFileChunks = `-- name: DeleteRepoIndexFileChunks :exec
DELETE FROM repo_index_chunk
WHERE workspace_id = $1
  AND repo_identifier = $2
  AND file_path = $3
`

type DeleteRepoIndexFileChunksParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	FilePath       string      `json:"file_path"`
}

// First half of the atomic per-file replace; InsertRepoIndexChunk is the
// second. Both run in one transaction so a file is never half-indexed.
func (q *Queries) DeleteRepoIndexFileChunks(ctx context.Context, arg DeleteRepoIndexFileChunksParams) error {
	_, err := q.db.Exec(ctx, deleteRepoIndexFileChunks, arg.WorkspaceID, arg.RepoIdentifier, arg.FilePath)
	return err
}

const diffRepoIndexFiles = `-- name: DiffRepoIndexFiles :many

SELECT DISTINCT file_path, content_hash
FROM repo_index_chunk
WHERE workspace_id = $1
  AND repo_identifier = $2
`

type DiffRepoIndexFilesParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
}

type DiffRepoIndexFilesRow struct {
	FilePath    string `json:"file_path"`
	ContentHash string `json:"content_hash"`
}

// Shared semantic repo index (K47). See migration 711 for the table's contract:
// lexical always, vector only when an embeddings endpoint is configured, and
// content_hash is the whole FILE's sha256 because a file is re-chunked as a
// whole.
// The (path, hash) pairs already indexed for one repo. The daemon subtracts
// this from what it found on disk to decide which files to re-chunk.
func (q *Queries) DiffRepoIndexFiles(ctx context.Context, arg DiffRepoIndexFilesParams) ([]DiffRepoIndexFilesRow, error) {
	rows, err := q.db.Query(ctx, diffRepoIndexFiles, arg.WorkspaceID, arg.RepoIdentifier)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DiffRepoIndexFilesRow{}
	for rows.Next() {
		var i DiffRepoIndexFilesRow
		if err := rows.Scan(&i.FilePath, &i.ContentHash); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertRepoIndexChunk = `-- name: InsertRepoIndexChunk :exec
INSERT INTO repo_index_chunk (
    workspace_id, repo_identifier, file_path, symbol,
    start_line, end_line, content, content_hash, embedding, embedding_model, indexed_commit
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8, CAST($10::text AS vector),
    $11::text, $9
)
`

type InsertRepoIndexChunkParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	FilePath       string      `json:"file_path"`
	Symbol         string      `json:"symbol"`
	StartLine      int32       `json:"start_line"`
	EndLine        int32       `json:"end_line"`
	Content        string      `json:"content"`
	ContentHash    string      `json:"content_hash"`
	IndexedCommit  string      `json:"indexed_commit"`
	Embedding      pgtype.Text `json:"embedding"`
	EmbeddingModel pgtype.Text `json:"embedding_model"`
}

// embedding is text-cast rather than a typed parameter: NULL stays NULL (the
// row is then lexical-only) and a present value arrives as pgvector's own
// '[a,b,c]' literal, which Postgres parses. A wrong-width vector is rejected
// here by the column, which is the intended failure — see migration 711.
func (q *Queries) InsertRepoIndexChunk(ctx context.Context, arg InsertRepoIndexChunkParams) error {
	_, err := q.db.Exec(ctx, insertRepoIndexChunk,
		arg.WorkspaceID,
		arg.RepoIdentifier,
		arg.FilePath,
		arg.Symbol,
		arg.StartLine,
		arg.EndLine,
		arg.Content,
		arg.ContentHash,
		arg.IndexedCommit,
		arg.Embedding,
		arg.EmbeddingModel,
	)
	return err
}

const pruneRepoIndexPaths = `-- name: PruneRepoIndexPaths :exec
DELETE FROM repo_index_chunk
WHERE workspace_id = $1
  AND repo_identifier = $2
  AND file_path <> ALL($3::text[])
`

type PruneRepoIndexPathsParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	PresentPaths   []string    `json:"present_paths"`
}

// Drops the chunks of files that no longer exist on the repo's default branch.
// An empty present_paths would wipe the repo, so the caller must never send one
// for a repo it failed to walk; service.PruneRepoIndex refuses that explicitly.
func (q *Queries) PruneRepoIndexPaths(ctx context.Context, arg PruneRepoIndexPathsParams) error {
	_, err := q.db.Exec(ctx, pruneRepoIndexPaths, arg.WorkspaceID, arg.RepoIdentifier, arg.PresentPaths)
	return err
}

const purgeRepoIndexChunksForRepo = `-- name: PurgeRepoIndexChunksForRepo :exec
DELETE FROM repo_index_chunk
WHERE workspace_id = $1
  AND repo_identifier = $2
`

type PurgeRepoIndexChunksForRepoParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
}

// Turning the index off for a repo in Settings deletes its chunks: the toggle
// is the workspace's answer about whether this code may be stored at all, so
// "off" must not leave the body text sitting in the table.
func (q *Queries) PurgeRepoIndexChunksForRepo(ctx context.Context, arg PurgeRepoIndexChunksForRepoParams) error {
	_, err := q.db.Exec(ctx, purgeRepoIndexChunksForRepo, arg.WorkspaceID, arg.RepoIdentifier)
	return err
}

const purgeWorkspaceRepoIndexChunks = `-- name: PurgeWorkspaceRepoIndexChunks :exec
DELETE FROM repo_index_chunk WHERE workspace_id = $1
`

func (q *Queries) PurgeWorkspaceRepoIndexChunks(ctx context.Context, workspaceID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, purgeWorkspaceRepoIndexChunks, workspaceID)
	return err
}

const queryRepoIndex = `-- name: QueryRepoIndex :many
WITH query AS (
    -- plainto_tsquery ANDs its terms, which is the wrong join for this query:
    -- the search text is a whole issue title plus the head of its description,
    -- and no single code chunk contains every word of an issue. Rewriting the
    -- conjunction into a disjunction keeps plainto_tsquery's stemming, stopword
    -- handling and escaping — the parts that are genuinely hard — while letting
    -- a chunk match on the words it does share. ts_rank then does the ordering:
    -- a chunk matching six of the terms outranks one matching two.
    SELECT replace(plainto_tsquery('english', $4::text)::text, '&', '|')::tsquery AS tsq
), newest AS (
    SELECT n.indexed_commit
    FROM repo_index_chunk n
    WHERE n.workspace_id = $5
      AND n.repo_identifier = $6
    ORDER BY n.updated_at DESC
    LIMIT 1
), lexical AS (
    SELECT
        c.id, c.file_path, c.symbol, c.start_line, c.end_line, c.content, c.indexed_commit,
        c.embedding, c.embedding_model,
        ts_rank(c.tsv, (SELECT query.tsq FROM query)) AS lex_rank,
        (CASE WHEN c.symbol <> '' AND c.symbol ILIKE '%' || $4::text || '%' THEN 1 ELSE 0 END
         + CASE WHEN c.file_path ILIKE '%' || $4::text || '%' THEN 1 ELSE 0 END)::float8 AS name_hits
    FROM repo_index_chunk c
    WHERE c.workspace_id = $5
      AND c.repo_identifier = $6
      AND (
        c.tsv @@ (SELECT query.tsq FROM query)
        OR (c.symbol <> '' AND c.symbol ILIKE '%' || $4::text || '%')
        OR c.file_path ILIKE '%' || $4::text || '%'
      )
    ORDER BY lex_rank DESC, name_hits DESC
    LIMIT $7::int
)
SELECT
    lexical.file_path,
    lexical.symbol,
    lexical.start_line,
    lexical.end_line,
    lexical.content,
    lexical.indexed_commit,
    (lexical.indexed_commit <> COALESCE((SELECT newest.indexed_commit FROM newest), lexical.indexed_commit))::boolean AS stale,
    (lexical.lex_rank
        + lexical.name_hits * 0.25
        + CASE WHEN lexical.embedding_model = $1::text
               THEN COALESCE(1 - (lexical.embedding <=> CAST($2::text AS vector)), 0)
               ELSE 0 END * 2
    )::float8 AS score
FROM lexical
ORDER BY score DESC, lexical.file_path ASC, lexical.start_line ASC
LIMIT $3::int
`

type QueryRepoIndexParams struct {
	EmbeddingModel pgtype.Text `json:"embedding_model"`
	QueryEmbedding pgtype.Text `json:"query_embedding"`
	TopK           int32       `json:"top_k"`
	Query          string      `json:"query"`
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	Prefilter      int32       `json:"prefilter"`
}

type QueryRepoIndexRow struct {
	FilePath      string  `json:"file_path"`
	Symbol        string  `json:"symbol"`
	StartLine     int32   `json:"start_line"`
	EndLine       int32   `json:"end_line"`
	Content       string  `json:"content"`
	IndexedCommit string  `json:"indexed_commit"`
	Stale         bool    `json:"stale"`
	Score         float64 `json:"score"`
}

// Hybrid retrieval in one statement.
//
// Stage 1 (`lexical`) is the prefilter and the only stage a deployment without
// embeddings has: full-text rank over the chunk body, plus name matches on the
// symbol and the file path, which are what a short "where is X handled" query
// actually hits. It takes a wider slice than the caller asked for so stage 2
// has something to reorder.
//
// Stage 2 adds cosine proximity when BOTH a query embedding and the chunk's own
// embedding exist. `embedding <=> NULL` is NULL, so COALESCE(..., 0) leaves a
// lexical-only row at its lexical score rather than dropping it — that COALESCE
// is the whole reason lexical-only deployments keep working.
//
// `stale` compares the chunk's commit to the repo's newest indexed commit: a
// run is told when a hint predates the current index pass.
func (q *Queries) QueryRepoIndex(ctx context.Context, arg QueryRepoIndexParams) ([]QueryRepoIndexRow, error) {
	rows, err := q.db.Query(ctx, queryRepoIndex,
		arg.EmbeddingModel,
		arg.QueryEmbedding,
		arg.TopK,
		arg.Query,
		arg.WorkspaceID,
		arg.RepoIdentifier,
		arg.Prefilter,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QueryRepoIndexRow{}
	for rows.Next() {
		var i QueryRepoIndexRow
		if err := rows.Scan(
			&i.FilePath,
			&i.Symbol,
			&i.StartLine,
			&i.EndLine,
			&i.Content,
			&i.IndexedCommit,
			&i.Stale,
			&i.Score,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const repoIndexStats = `-- name: RepoIndexStats :one
SELECT
    COUNT(*)::bigint AS chunk_count,
    COUNT(DISTINCT file_path)::bigint AS file_count,
    COALESCE(MAX(updated_at), '-infinity'::timestamptz)::timestamptz AS last_indexed_at,
    COUNT(*) FILTER (
        WHERE embedding IS NOT NULL
          AND NOT COALESCE(embedding_model = $3::text, false)
    )::bigint AS unusable_embedding_count,
    COALESCE((
        SELECT newest.indexed_commit
        FROM repo_index_chunk newest
        WHERE newest.workspace_id = $1
          AND newest.repo_identifier = $2
        ORDER BY newest.updated_at DESC
        LIMIT 1
    ), '')::text AS last_indexed_commit
FROM repo_index_chunk
WHERE workspace_id = $1
  AND repo_identifier = $2
`

type RepoIndexStatsParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	EmbeddingModel pgtype.Text `json:"embedding_model"`
}

type RepoIndexStatsRow struct {
	ChunkCount             int64              `json:"chunk_count"`
	FileCount              int64              `json:"file_count"`
	LastIndexedAt          pgtype.Timestamptz `json:"last_indexed_at"`
	UnusableEmbeddingCount int64              `json:"unusable_embedding_count"`
	LastIndexedCommit      string             `json:"last_indexed_commit"`
}

// Per-repo summary for the Settings block: how much is indexed, at which
// commit, and when. The commit is the one carried by the most recently written
// chunk, which is also the commit staleness is measured against.
func (q *Queries) RepoIndexStats(ctx context.Context, arg RepoIndexStatsParams) (RepoIndexStatsRow, error) {
	row := q.db.QueryRow(ctx, repoIndexStats, arg.WorkspaceID, arg.RepoIdentifier, arg.EmbeddingModel)
	var i RepoIndexStatsRow
	err := row.Scan(
		&i.ChunkCount,
		&i.FileCount,
		&i.LastIndexedAt,
		&i.UnusableEmbeddingCount,
		&i.LastIndexedCommit,
	)
	return i, err
}

const stampRepoIndexCommit = `-- name: StampRepoIndexCommit :exec
UPDATE repo_index_chunk
SET indexed_commit = $3
WHERE workspace_id = $1
  AND repo_identifier = $2
  AND indexed_commit <> $3
`

type StampRepoIndexCommitParams struct {
	WorkspaceID    pgtype.UUID `json:"workspace_id"`
	RepoIdentifier string      `json:"repo_identifier"`
	IndexedCommit  string      `json:"indexed_commit"`
}

// Records that a completed pass verified every remaining chunk of this repo at
// one commit.
//
// Without this, `stale` would be useless. An incremental pass only rewrites the
// files that CHANGED, so every untouched file would keep the commit it was
// first indexed at, and a repository where one file moved would report its
// other five hundred as stale — a caveat on every hint, meaning nothing. The
// pass did look at those files: it compared their hashes and found them
// current, which is exactly the assertion "verified at this commit". Stale then
// means what it should: this chunk was not part of the last completed pass.
func (q *Queries) StampRepoIndexCommit(ctx context.Context, arg StampRepoIndexCommitParams) error {
	_, err := q.db.Exec(ctx, stampRepoIndexCommit, arg.WorkspaceID, arg.RepoIdentifier, arg.IndexedCommit)
	return err
}
