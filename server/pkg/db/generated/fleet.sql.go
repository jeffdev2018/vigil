// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: fleet.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listFleetAgentActivityDaily = `-- name: ListFleetAgentActivityDaily :many

SELECT
    atq.agent_id,
    (atq.completed_at AT TIME ZONE 'UTC')::date::text AS date,
    COUNT(*)::int AS task_count,
    COUNT(*) FILTER (WHERE atq.status = 'failed')::int AS failed_count
FROM agent_task_queue atq
JOIN agent a ON a.id = atq.agent_id
WHERE a.workspace_id = $1
  AND atq.status IN ('completed', 'failed')
  AND atq.completed_at IS NOT NULL
  AND atq.completed_at >= $2::timestamptz
GROUP BY atq.agent_id, (atq.completed_at AT TIME ZONE 'UTC')::date
ORDER BY atq.agent_id, date
`

type ListFleetAgentActivityDailyParams struct {
	WorkspaceID pgtype.UUID        `json:"workspace_id"`
	Since       pgtype.Timestamptz `json:"since"`
}

type ListFleetAgentActivityDailyRow struct {
	AgentID     pgtype.UUID `json:"agent_id"`
	Date        string      `json:"date"`
	TaskCount   int32       `json:"task_count"`
	FailedCount int32       `json:"failed_count"`
}

// Fleet reads (JEF-12). One query behind GET /api/fleet/status and
// /api/fleet/history.
// Per-(agent, UTC calendar day) terminal-task buckets, anchored on
// completed_at and windowed by @since.
//
// This exists alongside GetWorkspaceAgentActivity30d rather than reusing it:
// that feeder's buckets are DATE_TRUNC in the SESSION timezone (built for the
// Agents-list sparkline, which renders instants), and its window is hard-wired
// to 30 days. The fleet endpoints promise ?since= and label buckets with a
// calendar date an LLM consumer can quote — both need a UTC-anchored,
// caller-windowed aggregation, so the fold-and-trim happens in SQL here.
func (q *Queries) ListFleetAgentActivityDaily(ctx context.Context, arg ListFleetAgentActivityDailyParams) ([]ListFleetAgentActivityDailyRow, error) {
	rows, err := q.db.Query(ctx, listFleetAgentActivityDaily, arg.WorkspaceID, arg.Since)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListFleetAgentActivityDailyRow{}
	for rows.Next() {
		var i ListFleetAgentActivityDailyRow
		if err := rows.Scan(
			&i.AgentID,
			&i.Date,
			&i.TaskCount,
			&i.FailedCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
